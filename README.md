# Mini GitHub Actions + Pages Dashboard

Dieses Projekt zeigt:

- GitHub Actions
- automatisierte HTML-Generierung
- Deployment über GitHub Pages

Bei jedem Push auf `main` wird automatisch ein Dashboard erzeugt und veröffentlicht.